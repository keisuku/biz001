import fs from 'node:fs/promises';import * as XLSX from 'xlsx';await fs.mkdir('testdata',{recursive:true});const rows=[];for(let i=0;i<50000;i++){const r={'商品CD':i%17===0?'ZZZ':i%5===0?'Ｆ００１':i%5===1?'F123':i%5===2?'A123':String(1000+i%1000),'金額':i%13===0?'△1,234':i%7===0?'¥2,345':i};for(let c=0;c<48;c++)r['列'+String(c+1).padStart(2,'0')]=`値${i}-${c}`;rows.push(r)}const ws=XLSX.utils.json_to_sheet(rows);const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,'Sheet1');XLSX.writeFile(wb,'testdata/pivot-bench-50000.xlsx');
